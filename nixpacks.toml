[phases.setup]
nixPkgs = ["nodejs", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "pip install instagrapi --break-system-packages || pip3 install instagrapi"
]

[start]
cmd = "node server.js"
